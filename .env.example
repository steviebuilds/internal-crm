MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@cluster.mongodb.net/?appName=crm
MONGODB_DB=crm_v1
AUTH_SECRET=replace-with-a-long-random-secret
# Use either plain password (dev) OR a bcrypt hash (recommended production)
CRM_PASSWORD=changeme
# CRM_PASSWORD_HASH=$2b$12$...

# Legacy safety guard: fallback reads from `leads` only when explicitly enabled.
# ENABLE_LEGACY_LEADS_FALLBACK=1
